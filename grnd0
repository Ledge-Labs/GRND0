#!/bin/sh
# SPDX-License-Identifier: MPL-2.0
set -eu
if [ "${1:-}" != "models" ]; then
  echo "Usage: ./grnd0 models <pull|generate|status> <pack>" >&2
  exit 2
fi
shift
exec uv run python tools/grnd0_models.py "$@"
