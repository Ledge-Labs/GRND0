# Public evaluation contract

`result.schema.json` defines the submission record for public result entries.
Every record binds a suite to one configuration manifest, one hardware profile,
numeric scores, dataset revision, sample count, and immutable receipt references.
Records lacking any required binding are invalid.
